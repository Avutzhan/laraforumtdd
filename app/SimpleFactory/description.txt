Паттерн простая фабрика это класс в котором есть один метод с
большим условным оператором выбирающим создаваемый продукт
Этот метод вызывают с неким параметром по которому определяется
какой из продуктов нужно создать

У простой фабрики обычно нет подклассов обычно простую фабрику
путают с общим понятием фабрики или с любым из фабричных паттернов

ссылка на рефакторинг гуру

Простая фабрика просто генерирует экземпляр для клиента без
предоставления какой либо логики экземпляра

В ООП фабрикой называют обьект создающий другие обьекты
Формально фабрика это функция или метод возвращающая обьекты
разных прототипов или классов из вызова
какого то метода который считается новым

Вот снизу это бред ник солюшн и десинг паттернс сайта главное не запутаться
не подход всетаки а паттерн
ПФ позволяет создавать обьекты без логики их создания в клиенте
сам по себе не шаблон а подход как фундаментальный шаблон интерфейс
Это подход при котором логика создания обьектов выносится в отдельный класс

они утверждали что абстрактная фабрика === простая фабрика
Простая фабрика отличается от статической только одним что она не статическая
Простая должна быть всегда предпочтительнее чем статическая